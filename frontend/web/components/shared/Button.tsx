'use client';

// Legacy import location retained during the gradual migration. All shared
// buttons now use the same visual, keyboard, loading and disabled contract.
export { Button as default } from '@/components/ui/Button';
export type { ButtonProps } from '@/components/ui/Button';
