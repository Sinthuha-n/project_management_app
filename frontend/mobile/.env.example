EXPO_PUBLIC_API_BASE_URL=https://api.planora.app
# Use your local backend URL for development, for example http://localhost:8080.
# Production/preview builds should inject the stable HTTPS backend domain through EAS env/secrets.
